using System;
using System.Collections.Generic;

namespace PrimeNumbers
{
    public class PrimeNumberGenerator
    {
        public static IList<int> Generate(int limit)
        {
            var primes = new List<int>();

            if (limit == 1)
                return primes;


            return primes;

        }
    }
}